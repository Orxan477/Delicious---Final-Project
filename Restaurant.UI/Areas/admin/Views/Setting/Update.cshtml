@model Setting
@{
    ViewData["Title"] = "Update";
}

<row>
    <div class="col-md-6 grid-margin stretch-card">
        <div class="card">
            <div class="card-body mt-5">
                <h4 class="card-title">@ViewBag.Setting</h4>
                <form class="forms-sample d-inline" method="post">
                    <div class="form-group">
                        <input asp-for="Value" type="@Model.Type"class="form-control" placeholder="Value">
                        <span class="text-danger" asp-validation-for="Value"></span>
                    </div>
                    <button type="submit" class="btn btn-primary mr-2">Submit</button>
                </form>
                    <a class="btn btn-dark" asp-action="Index">Cancel</a>
            </div>
        </div>
    </div>
</row>
